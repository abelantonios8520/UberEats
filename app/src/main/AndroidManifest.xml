<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.abelsalcedo.ubereats">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.cliente.DetalleEntregaActivity"
            android:parentActivityName=".activities.cliente.PedidosActivity"></activity>
        <activity android:name=".activities.cliente.PedidosActivity"/>
        <activity android:name=".activities.ForgotPassActivity" />
        <activity android:name=".activities.delivery.MapDeliveryActivity" />
        <activity
            android:name=".activities.cliente.MapClienteActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.loginActivity"
            android:parentActivityName=".activities.SelectOptionAuthActivity" />
        <activity
            android:name=".activities.delivery.Register_delivery_Activity"
            android:parentActivityName=".activities.SelectOptionAuthActivity" />
        <activity
            android:name=".activities.cliente.RegisterActivity"
            android:parentActivityName=".activities.SelectOptionAuthActivity" />
        <activity
            android:name=".activities.SelectOptionAuthActivity"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:parentActivityName=".activities.splash" />
        <activity android:name=".activities.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB0_iwpIHZQX6slFqsZY9Ari3q6oUHre3w" />
    </application>

</manifest>